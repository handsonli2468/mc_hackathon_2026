# Human clarification when grounding is insufficient
Ask the user only for information that truly blocks planning. If an object can be searched with open-vocabulary perception, its unknown current location is normally not a blocking question. For a delivery recipient such as "me", ask for a visual or spatial clue that can identify the recipient if none is available.
